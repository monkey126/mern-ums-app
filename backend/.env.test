# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test MongoDB Configuration (use main database with test prefix)
MONGODB_URI=mongodb+srv://rashedulhasan:S01673591736s@cluster0.kf0u1d6.mongodb.net/mern-ums-app?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (same as development for testing)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRE=1h
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-only
JWT_REFRESH_EXPIRE=24h

# Email Configuration (disabled for testing)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASS=test-password
EMAIL_FROM=noreply@test.com

# Frontend URL (for CORS in tests)
FRONTEND_URL=http://localhost:5173

# Cloudinary Configuration (disabled for testing)
CLOUDINARY_CLOUD_NAME=test-cloud
CLOUDINARY_API_KEY=test-api-key
CLOUDINARY_API_SECRET=test-api-secret